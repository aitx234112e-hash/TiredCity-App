<?xml version="1.0" encoding="utf-8"?>
<!-- Nền thẻ Sự kiện / Tạp chí: trắng mờ như thẻ Ưu đãi nhưng viền đỏ son nhạt -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/tc_reward_card_bg" />
    <stroke
        android:width="1.5dp"
        android:color="@color/tc_event_card_stroke" />
    <corners android:radius="18dp" />
</shape>
