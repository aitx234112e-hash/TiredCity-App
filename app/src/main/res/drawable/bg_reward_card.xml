<?xml version="1.0" encoding="utf-8"?>
<!-- Nền thẻ ưu đãi: trắng, viền mờ gần như không thấy -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/tc_reward_card_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/tc_reward_card_stroke" />
    <corners android:radius="18dp" />
</shape>
