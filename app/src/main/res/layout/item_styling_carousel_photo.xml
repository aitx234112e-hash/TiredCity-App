<?xml version="1.0" encoding="utf-8"?>
<!-- Một trang của carousel banner ảnh trang phục (Horizontal Scroll Section) ở đầu trang "Danh
     mục" — ảnh phủ kín toàn bộ chiều rộng, không viền/bo góc, giống 1 banner quảng cáo ngang. -->
<ImageView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/iv_carousel_photo"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:scaleType="centerCrop"
    tools:src="@android:drawable/ic_menu_gallery"
    android:contentDescription="@null" />
